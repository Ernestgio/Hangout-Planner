pre-commit:
  parallel: true
  commands:
    shared-go-mod-tidy:
      run: cd pkg/shared && go mod tidy

    shared-golangci-lint:
      run: cd pkg/shared && golangci-lint run ./...

    shared-go-test:
      run: cd pkg/shared && go test -v ./...
