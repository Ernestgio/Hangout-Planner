pre-commit:
  parallel: true
  commands:
    hangout-service-go-mod-tidy:
      run: cd services/hangout && go mod tidy

    hangout-service-go-test:
      run: cd services/hangout && go test ./... -cover

    hangout-service-golangci-lint:
      run: cd services/hangout && golangci-lint run ./...
